import { Box } from 'components'
import styled from 'styled-components'
const InputFormWrapper = styled(Box)`
  background-color: var(--smart-form-bg);
  height: 46px;
  display: flex;
  align-items: center;
`

export { InputFormWrapper }
